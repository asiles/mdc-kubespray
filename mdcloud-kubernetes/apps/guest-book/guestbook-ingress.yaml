apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: guestbook-ingress
  labels:
    app: guestbook
spec:
  rules:
  - host: guestbook.k8s.mediacloud.lab
    http:
      paths:
      - path: /
        backend:
          serviceName: frontend
          servicePort: 80